version: "3"

services: 
    server:
        build: server/
        command: python3 ./video.py

        environment:
            DISPLAY: $DISPLAY
        volumes:
            - /tmp/.X11-unix:/tmp/.X11-unix
            - ~/.Xauthority:/root/.Xauthority

        network_mode: "host"


